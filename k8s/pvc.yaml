apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-models
  namespace: ollama
spec:
  storageClassName: synology-nfs
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
